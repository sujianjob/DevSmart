# 产品交付流程

## POC 流程

```text
输入需求
  -> 模糊度判断
  -> 生成澄清问题
  -> 用户补充
  -> 生成 PRD
  -> 生成 AC
  -> 自动补漏
  -> 汇总待确认项
  -> 评审输出
```

## 关键原则

- 信息不足时先澄清，不直接生成完整 PRD。
- 所有 AI 推测必须标记为假设。
- AC 必须可测试、可判断。
- 非目标和边界场景必须显式列出。
- 评审输出只展示可审计的输入、输出和修改记录，不展示模型内部推理链。
