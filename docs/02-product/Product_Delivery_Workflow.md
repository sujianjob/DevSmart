# 产品交付流程

## POC 流程

```text
输入需求
  -> 生成 PRD
  -> 人工审批
  -> 拆解任务
  -> 生成上下文包
  -> 分发给人或外部 Coding Agent
  -> 回收结果
  -> 生成 QA 建议
  -> 流程回放
```

## 关键原则

- PRD 未审批不得进入任务拆解。
- 上下文包必须包含验收标准。
- 外部执行结果必须回写摘要。
- CI/CD 只读接入，不自动触发。
- 流程回放只展示可审计事件，不展示内部推理链。
