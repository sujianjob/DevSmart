# 权限矩阵

POC 阶段只保留最小权限模型。

| 角色 | 权限 |
|:---|:---|
| Owner | 管理项目、成员、Agent 配置和全部数据 |
| Admin | 创建需求、审批 PRD、分配任务、查看审计 |
| Member | 创建需求、查看任务、接收上下文包 |
| Guest | 只读查看被授权项目 |

## POC 约束

- 不做复杂组织树。
- 不做计费权限。
- 不做字段级权限。
- 所有审批动作必须记录审计事件。

生产阶段再扩展 SSO、SCIM、细粒度 RBAC 和私有化部署策略。
