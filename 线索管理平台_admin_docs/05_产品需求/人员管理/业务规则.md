# 人员管理模块 - 业务规则

## 一、校验规则 (Validation Rules)

### VR-001: 用户名不能为空

| 项目 | 说明 |
|-----|------|
| 规则编号 | VR-001 |
| 适用场景 | 内部用户关联 |
| 规则描述 | 用户名不能为空 |
| 代码位置 | `AuthInnerUserServiceImpl.associated()` Line 98-100 |
| 异常信息 | "用户名不能为空！" |

```java
if (StringUtils.isBlank(request.getUserName())) {
    throw new BaseServiceException("500", "用户名不能为空！");
}
```

### VR-002: 关联渠道必填

| 项目 | 说明 |
|-----|------|
| 规则编号 | VR-002 |
| 适用场景 | 内部用户关联 |
| 规则描述 | 关联渠道和关联渠道ID不能为空 |
| 代码位置 | `AuthInnerUserServiceImpl.associated()` Line 102-104 |
| 异常信息 | "关联渠道不能为空！" |

### VR-003: 主次加微号不能重复

| 项目 | 说明 |
|-----|------|
| 规则编号 | VR-003 |
| 适用场景 | 内部用户关联 |
| 规则描述 | 主加微号和次加微号不能选择相同的账号 |
| 代码位置 | `AuthInnerUserServiceImpl.associated()` Line 110-114 |
| 异常信息 | "主次加微号不能重复！" |

```java
if (StringUtils.isNotBlank(request.getPrimaryWechatAccountId())
    && StringUtils.isNotBlank(request.getSecondaryWechatAccountId())) {
    if (request.getPrimaryWechatAccountId().equals(request.getSecondaryWechatAccountId())) {
        throw new BaseServiceException("500", "主次加微号不能重复！");
    }
}
```

### VR-004: 外部用户新增必填校验

| 项目 | 说明 |
|-----|------|
| 规则编号 | VR-004 |
| 适用场景 | 外部用户新增 |
| 规则描述 | 用户名、账号、密码、状态、角色、公司为必填 |
| 代码位置 | `AuthVendorUserServiceImpl.add()` Line 51-53 |
| 异常信息 | 参数有误 (PARAM_FAIL) |

```java
if (StringUtils.isAnyEmpty(request.getUserName(), request.getUserAccount(),
    request.getPassword(), request.getStatus(), request.getRole(), request.getCompanyId())) {
    throw new BaseServiceException(BaseResultCode.PARAM_FAIL);
}
```

### VR-005: 账号唯一性校验

| 项目 | 说明 |
|-----|------|
| 规则编号 | VR-005 |
| 适用场景 | 外部用户新增 |
| 规则描述 | 用户账号不能重复 |
| 代码位置 | `AuthVendorUserServiceImpl.checkUserAccount()` Line 151-159 |
| 异常信息 | 用户账号已存在 (USER_ACCOUNT_EXIST) |

### VR-006: 修改时ID必填

| 项目 | 说明 |
|-----|------|
| 规则编号 | VR-006 |
| 适用场景 | 外部用户修改 |
| 规则描述 | 修改用户时必须提供用户ID |
| 代码位置 | `AuthVendorUserServiceImpl.update()` Line 71-73 |

### VR-007: 密码一致性校验（前端）

| 项目 | 说明 |
|-----|------|
| 规则编号 | VR-007 |
| 适用场景 | 外部用户新增/编辑 |
| 规则描述 | 两次输入的密码必须一致 |
| 代码位置 | `addUser.vue` 前端校验 |
| 异常信息 | "两次输入密码不一致" |

---

## 二、状态规则 (Status Rules)

### SR-001: 账号存在性校验

| 项目 | 说明 |
|-----|------|
| 规则编号 | SR-001 |
| 适用场景 | 用户登录 |
| 规则描述 | 登录时账号必须存在 |
| 代码位置 | `AuthVendorUserServiceImpl.userLogin()` |
| 异常信息 | 用户账号不存在 (USER_ACCOUNT_NOT_EXIST) |

### SR-002: 账号禁用状态校验

| 项目 | 说明 |
|-----|------|
| 规则编号 | SR-002 |
| 适用场景 | 用户登录 |
| 规则描述 | 禁用状态的账号不能登录 |
| 代码位置 | `AuthVendorUserServiceImpl.userLogin()` |
| 异常信息 | 账号已禁用 (USER_ACCOUNT_DISABLE) |

```java
if (BaseStatusEnum.DISABLE.getCode().equals(authVendorUser.getStatus())) {
    throw new BaseServiceException(BaseResultCode.USER_ACCOUNT_DISABLE);
}
```

---

## 三、系统行为规则 (System Behavior)

### SB-001: 内部用户关联覆盖式保存

| 项目 | 说明 |
|-----|------|
| 规则编号 | SB-001 |
| 规则描述 | 关联账号时采用先删除后新增的策略 |
| 代码位置 | `AuthInnerUserServiceImpl.associated()` Line 117-127 |

```java
// 先删除已有记录
AuthInnerUserDOExample exampleDelete = new AuthInnerUserDOExample();
exampleDelete.createCriteria().andUserNameEqualTo(request.getUserName());
authInnerUserMapper.deleteByExample(exampleDelete);
// 然后新增
authInnerUserMapper.insertSelective(authInnerUserDO);
```

### SB-002: 密码加密规则

| 项目 | 说明 |
|-----|------|
| 规则编号 | SB-002 |
| 规则描述 | 密码使用 MD5 + 盐值 + 2次迭代加密 |
| 代码位置 | `AuthVendorUserServiceImpl` |

```java
public static String getPwd(String password) {
    return new SimpleHash("md5", password, ByteSource.Util.bytes("123456"), 2).toString();
}
```

### SB-003: 默认状态设置

| 项目 | 说明 |
|-----|------|
| 规则编号 | SB-003 |
| 规则描述 | 新增内部用户时默认启用状态 |
| 默认值 | EnableEnum.ENABLE.getCode() = "1" |

### SB-004: 自动记录时间戳

| 项目 | 说明 |
|-----|------|
| 规则编号 | SB-004 |
| 规则描述 | 新增/修改时自动设置创建/更新时间 |

```java
authInnerUserDO.setCreateTime(new Date());
authInnerUserDO.setUpdateTime(new Date());
```

---

## 四、错误码对照

| 错误码 | 常量名 | 说明 |
|-------|-------|------|
| 410001 | PARAM_FAIL | 参数有误 |
| 410006 | USER_ACCOUNT_EXIST | 用户账号已存在 |
| 410007 | USER_ACCOUNT_NOT_EXIST | 用户账号不存在 |
| 410011 | USER_ACCOUNT_DISABLE | 账号已禁用 |

---

## 五、状态枚举

### EnableEnum

| 枚举值 | Code | 说明 |
|-------|------|------|
| ENABLE | "1" | 启用 |
| DISABLE | "0" | 禁用 |

### VendorUserRoleEnum

| 枚举值 | Code | 说明 |
|-------|------|------|
| BPO | "BPO" | BPO角色 |
| BPO_ADMIN | "BPO_ADMIN" | BPO管理员 |
